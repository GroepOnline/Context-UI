{
  "name": "pi-context-extension",
  "version": "1.0.0",
  "description": "Pi.dev extension for context status and token estimation",
  "main": "dist/extension.js",
  "types": "dist/extension.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "pi",
    "pi.dev",
    "extension",
    "context",
    "tokens"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^4.1.7",
    "typescript": "^5.0.0",
    "vitest": "^4.1.7"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "pi": {
    "extension": true,
    "commands": [
      "/context",
      "/ctx",
      "/tokens"
    ]
  }
}
